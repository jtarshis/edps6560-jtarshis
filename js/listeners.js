$(document).ready(() => {

input_btn_about.addEventListener("hover",function(){
    
});

$(input_btn_about).on('mouseenter', () => {
    $('.about-menu').show()
})

$('.about-menu').on('mouseleave', () => {
    $('.about-menu').hide();
}) 
    
});